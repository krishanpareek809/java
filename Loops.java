import java.util.Scanner;

public class Loops {
    public static void main(String[] args) {
        // int n;
        // int i;
        // Scanner sc = new Scanner(System.in);
        // System.out.print("Enter the value of n: ");
        // n = sc.nextInt();

        for(int i = 4; i>=0; i--){
            System.out.println(i);
        }

        // break is used to exit the loop whether the condition is true or false, whenever a "break" is encountered inside the loop, the control is sent outside the loop
        // continue is used to immediately move to the next itreration of the loop, thus, skipping everything below "continue" inside the loop for that iteration
    }
}